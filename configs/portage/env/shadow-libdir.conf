# shadow installs libsubid to $(libdir) = /lib, but modern Gentoo (merged-usr)
# requires everything under /usr/. Override libdir at configure time so
# libsubid.a lands in /usr/lib/ and passes the QA check.
EXTRA_ECONF="${EXTRA_ECONF} --libdir=/usr/lib"
